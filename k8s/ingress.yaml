apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: query-ingress
  namespace: petshop-apps
spec:
  rules:
    - http:
        paths:
          - path: /api/query
            pathType: Prefix
            backend:
              service:
                name: query-service
                port:
                  number: 8080
